# @PROJECT_NAME@

Generated with JDesk's `basic` template — plain HTML/CSS/JS, no Node or bundler required.

```bash
./gradlew run        # build the UI and launch the app
./gradlew jdeskDev   # dev loop: reloads the page on UI changes, restarts on Java changes
```

In the dev loop, edits under `ui/` are rebuilt and the window reloads automatically.
Changes under `src/main/java` or `src/main/resources` are rebuilt and restart the Java
application after a successful compile.
