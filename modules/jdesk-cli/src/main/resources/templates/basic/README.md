# @PROJECT_NAME@

Generated with JDesk's `basic` template.

```bash
./gradlew classes
npm install --prefix ui
./gradlew jdeskDev
```

The Vite server updates frontend code through HMR. Changes under `src/main/java` or
`src/main/resources` are rebuilt and restart the Java application after a successful compile.
